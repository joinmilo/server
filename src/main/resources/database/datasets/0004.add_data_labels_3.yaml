databaseChangeLog:
  - logicalFilePath: path_independent
         
  - changeSet:
      id: 0004.add_data_labels_3.1
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noCurrentSurveyComeLater";
               SET @german  = "Es gibt keine aktuellen Umfragen. Besuche die Seite zu einem späteren Zeitpunkt";
               SET @english = "There are no current polls. Visit the site at a later time";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.2
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "pastSurveys";
               SET @german  = "Vergangene Umfragen";
               SET @english = "past polls";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.3
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "surveys";
               SET @german  = "Umfragen";
               SET @english = "Polls";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.4
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createGuestArticle";
               SET @german  = "Gastartikel erstellen";
               SET @english = "create guest post";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0004.add_data_labels_3.5
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "guestArticleText";
               SET @german  = "Teile deine Stimme und Perspektive, indem du einen Gastbeitrag auf unserer Website verfasst. Egal, ob du Experte bist oder deine Gedanken teilen möchtest - wir möchten deine Beiträge sehen! Veröffentliche deinen Gastbeitrag einfach, ohne einen Account erstellen oder dich anmelden zu müssen.";
               SET @english = "Share your voice and perspective by guest posting on our site. It doesn't matter if you're an expert or want to share your thoughts - we want to see your posts! Publish your guest post easily, without creating an account or signing in.";



               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.6
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noContestsChangeFilterOrComeLater";
               SET @german  = "Leider finden derzeit keine Wettbewerbe statt. Ändern Sie die Filter oder besuchen Sie die Wettbewerbseite zu einem anderen Zeitpunkt.";
               SET @english = "Unfortunately there are currently no contests taking place. Change the filters or visit the contest page at a different time.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.7
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "contests";
               SET @german  = "Wettbewerbe";
               SET @english = "Contests";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.8
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "activeContests";
               SET @german  = "Wettbewerb teilnehmen";
               SET @english = "Enter Contest";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.9
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "completedContests";
               SET @german  = "Vergangene Wettbewerbe";
               SET @english = "Past Contests";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.10
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "voteableContests";
               SET @german  = "Abstimmungen";
               SET @english = "Votes";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.11
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "contestTypes";
               SET @german  = "Wettbewerbsarten";
               SET @english = "contest types";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
              
  - changeSet:
      id: 0004.add_data_labels_3.12
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "sponsoredContent";
               SET @german  = "Gesponserte Inhalte";
               SET @english = "Sponsored content";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.13
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "thankYou";
               SET @german  = "Vielen Dank für deinen Gastbeitrag!";
               SET @english = "Thank you for your guest post!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.14
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "guestArticleSuccesMessage";
               SET @german  = "Herzlichen Glückwunsch! Du hast deinen Gastbeitrag erfolgreich auf unserem Portal hochgeladen. Bitte habe noch etwas Geduld, während unser Team den Text prüft und ihn baldmöglichst veröffentlicht.";
               SET @english = "Congratulations! You have successfully uploaded your guest post to our portal. Please be patient while our team reviews the text and publishes it as soon as possible.!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.15
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeContentCreaterText";
               SET @german  = "Falls du in Zukunft weitere Beiträge schreiben möchtest, kannst du dich auf unserem Portal als Verfasser:in registrieren lassen. Sobald du registriert bist, hast du die Möglichkeit, eine Anfrage zu stellen, um als Publizierende:r anerkannt zu werden und Inhalte zu erstellen.";
               SET @english = "If you would like to write more articles in the future, you can register as an author on our portal. Once registered, you have the option to submit a request to be recognized as a publisher and start creating content.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.16
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noFavorites";
               SET @german  = "Du hast zu dieser Kategorie noch keine Favoriten gespeichert.";
               SET @english = "You have not yet saved any favorites for this category.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.17
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "friends";
               SET @german  = "Freunde";
               SET @english = "Friends";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.18
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "allFriends";
               SET @german  = "Alle Freunde";
               SET @english = "All Friends";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.19
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "receivedFriendRequests";
               SET @german  = "Erhaltene Anfragen";
               SET @english = "Received requests";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.20
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "sentFriendRequests";
               SET @german  = "Versendete Anfragen";
               SET @english = "Requests sent";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.21
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "addFriends";
               SET @german  = "Freunde hinzufügen";
               SET @english = "add Friends";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.22
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "findYourFriends";
               SET @german  = "Finde deine Freunde im Portal.";
               SET @english = "Find your friends in the portal.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.23
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "showFriendsAtEvents";
               SET @german  = "Deine Freunde werden bei Events angezeigt, sobald sie diese geliked haben. Auch deine Freunde sehen dich, wenn du an einem Event teilnimmst.";
               SET @english = "Your friends will appear in events as soon as they have liked them. Your friends will also see you when you attend an event.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.24
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "emailAddress";
               SET @german  = "E-Mail-Addresse";
               SET @english = "email address";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.25
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "sendFriendRequest";
               SET @german  = "Freundschaftsanfrage versenden";
               SET @english = "Send friend request";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.26
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "friendRequestsSent";
               SET @german  = "Deine Anfragen wurden erfolgreich versendet.";
               SET @english = "Your requests have been sent successfully.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.27
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "friendRequestSent";
               SET @german  = "Deine Anfrage wurde erfolgreich versendet.";
               SET @english = "Your request have been sent successfully.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.28
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "removeFriend";
               SET @german  = "Freundschaft entfernen";
               SET @english = "Remove friend";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.29
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "confirm";
               SET @german  = "Bestätigen";
               SET @english = "Confirm";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.30
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "remove";
               SET @german  = "Entfernen";
               SET @english = "Remove";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
               
  - changeSet:
      id: 0004.add_data_labels_3.31
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noFriends";
               SET @german  = "Du hast noch keine Freunde hinzugefügt";
               SET @english = "You haven't added any friends yet";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.32
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noFriendRequests";
               SET @german  = "Du hast keine Freundschaftsanfragen erhalten";
               SET @english = "You have not received any friend requests";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.33
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noSentRequests";
               SET @german  = "Du hast keine Freundschaftsanfragen versendet";
               SET @english = "You haven't sent any friend requests";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
  - changeSet:               
      id: 0004.add_data_labels_3.34
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "participate";
               SET @german  = "Mitmachen";
               SET @english = "Participate";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0004.add_data_labels_3.35
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createOrganisation";
               SET @german  = "Einrichtung erstellen";
               SET @english = "Create Organisation";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.36
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthor";
               SET @german  = "Publizierende:r werden";
               SET @english = "Become a author";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.37
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinOrganisation";
               SET @german  = "Einrichtung beitreten";
               SET @english = "Join Organisation";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.38
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorText";
               SET @german  = "Du möchtest Publizierende:r werden? Dann stell hier deinen Antrag, um deine interessanten Beiträge mit der Community zu teilen.";
               SET @english = "Would you like to become a author? Then submit your application here to share your interesting contributions with the community.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.39
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorAdvantages";
               SET @german  = "Welche Vorteile hast du als Publizierende:r?";
               SET @english = "What advantages do you have as an author?";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.40
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorAdvantages1";
               SET @german  = "Du erhältst ein Dashboard in welchem du deine wichtigsten Kennzahlen auf einem Blick siehst.";
               SET @english = "You will receive a dashboard in which you can see your most important key figures at a glance.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.41
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorAdvantages2";
               SET @german  = "Du hast all deine Artikel auf einem Blick und kannst diese im Nachhinein bearbeiten.";
               SET @english = "You have all your articles at a glance and can edit them afterwards.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.42
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorAdvantages3";
               SET @german  = "Du bekommst ein eigenes Profi, wodurch dir die Community folgen kann.";
               SET @english = "You get your own professional, which allows the community to follow you.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.43
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "join";
               SET @german  = "Beitreten";
               SET @english = "Join";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
               
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0004.add_data_labels_3.44
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "dataProtection";
               SET @german  = "Datenschutz";
               SET @english = "Data Protection";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.45
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "profileData";
               SET @german  = "Profildaten";
               SET @english = "Profile Data";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.46
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "emailNotifications";
               SET @german  = "E-Mail";
               SET @english = "Email";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.47
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "pushNotifications";
               SET @german  = "Push";
               SET @english = "Push";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.48
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "personalData";
               SET @german  = "Persönliche Daten";
               SET @english = "Personal Data";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.49
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "changePassword";
               SET @german  = "Passwort ändern";
               SET @english = "Change Password";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.50
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deleteAccount";
               SET @german  = "Konto löschen";
               SET @english = "Delete Account";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.51
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deleteAccountText";
               SET @german  = "Wir bedauern, dass du dein Konto löschen möchtest. Um unseren Service zu verbessern, würden wir uns über deine Rückmeldungen zu den Gründen für die Löschung Ihres Kontos sehr freuen. Deine Information hilft uns, unseren Service zu optimieren und auf die Bedürfnisse unserer Nutzer:innen besser einzugehen.";
               SET @english = "We are sorry that you want to delete your account. In order to improve our service, we would appreciate your feedback on the reasons for deleting your account. Your information helps us to optimize our service and to better respond to the needs of our users.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.52
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "reason1";
               SET @german  = "Du hast deinen Wohnort gewechselt.";
               SET @english = "You have changed your place of residence.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.53
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "continue";
               SET @german  = "Weiter";
               SET @english = "Continue";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.54
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deleteAccountText1";
               SET @german  = "Optional kannst du uns hier detailliert mitteilen, weshalb du deinen Account löschen möchtest.";
               SET @english = "Optionally, you can tell us in detail here why you want to delete your account.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.55
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "reasonsTextarea";
               SET @german  = "Detailliert Beschreibung.";
               SET @english = "Detailed description.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.56
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deleteAccountText2";
               SET @german  = "Damit du deinen Account löschen kannst, musst du hier noch einmal dein Passwort eingeben.";
               SET @english = "In order to be able to delete your account, you have to enter your password again here.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.57
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deleteAccountText3";
               SET @german  = "Sobald dein Konto gelöscht worden ist, sind alle deine Daten von unseren Servern gelöscht und die Wiederherstellung deines Accounts ist nicht mehr möglich.";
               SET @english = "Once your account has been deleted, all your data will be deleted from our servers and it will no longer be possible to recover your account.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.58
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "repeatPassword";
               SET @german  = "Passwort wiederholen";
               SET @english = "Repeat password";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.59
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "changePasswordText";
               SET @german  = "Hier kannst du dein neues Passwort setzen.";
               SET @english = "You can set your new password here.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.60
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "passwordChanged";
               SET @german  = "Dein Passwort wurde erfolgreich geändert! Bitte melde dich erneut an.";
               SET @english = "Your password has been successfully changed! Please log in again.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.61
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createYourOrganisation";
               SET @german  = "Erstelle deine Einrichtung!";
               SET @english = "Create Your Organisation!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.62
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "organisationText1";
               SET @german  = "Werde Teil unseres Portal und erhöhe deine Reichweite!";
               SET @english = "Become part of our portal and increase your reach!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.63
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "organisationText2";
               SET @german  = "Erstelle deine Einrichtung in unserem Portal und verbinden dich mit Gleichgesinnten und Neugierigen.";
               SET @english = "Create your institution in our portal and connect with like-minded and curious people.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.64
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "describeYourOrganisation";
               SET @german  = "Beschreibe deine Einrichtung";
               SET @english = "Describe your Organisation";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.65
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "profilePicture";
               SET @german  = "Profilbild";
               SET @english = "Profile Picture";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.66
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "titelImage";
               SET @german  = "Titelbild";
               SET @english = "Titel Image";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.67
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "housenumber";
               SET @german  = "Hausnr.";
               SET @english = "HN";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.68
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "city";
               SET @german  = "Stadt";
               SET @english = "City";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.69
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "postalcode";
               SET @german  = "PLZ";
               SET @english = "PC";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.70
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "describtion";
               SET @german  = "Beschreibung";
               SET @english = "Describtion";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.71
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "clearAll";
               SET @german  = "Alles löschen";
               SET @english = "Clear all";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.72
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "enterYourAddress";
               SET @german  = "Trage deine Adresse ein";
               SET @english = "Enter your address";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.73
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinIn";
               SET @german  = "Mach mit!";
               SET @english = "Join in!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.74
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinOrganisationText1";
               SET @german  = "Gemeinsam schaffen wir mehr! Finde deine Einrichtung/en und gestalte das Programm mit.";
               SET @english = "Together we can do more! Find your organisation/s and help shaping the program.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
                             
  - changeSet:
      id: 0004.add_data_labels_3.75
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinOrganisationText2";
               SET @german  = "Mitmachen können alle, die Lust auf neues und gemeinschaftlichen Aktivitäten haben.";
               SET @english = "Everyone who is interested in new and joint activities can take part.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.76
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "thankYouForParticipating";
               SET @german  = "Vielen Dank fürs Mitmachen!";
               SET @english = "Thank you for participating!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.77
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeAuthorSuccessText";
               SET @german  = "Vielen Dank für deine Bewerbung als Publizierende:r. Wir werden uns in Kürze bei dir über das Portal melden.";
               SET @english = "Thank you for your application as an author. We will be in touch with you shortly via the portal.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.78
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinOrganisationSuccessText";
               SET @german  = "Vielen Dank für Anfrage. Die bestimmte/n Einrichtung/en werden sich in Kürze bei dir über das Portal melden.";
               SET @english = "Thank you for inquiry. The specific organisation(s) will be in touch with you shortly via the portal.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.79
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createOrganisationSuccessText";
               SET @german  = "Vielen Dank für das Erstellen einer Einrichtung. Wir werden deine Angaben prüfen und in Kürze bei dir über das Portal melden.";
               SET @english = "Thank you for creating an organisation. We will check your details and get back to you via the portal shortly.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.80
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "userDeleted";
               SET @german  = "Der Account wurde erfolgreich gelöscht.";
               SET @english = "The account has been successfully deleted.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.81
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "personalDataSaved";
               SET @german  = "Deine Daten wurde erfolgreich gespeichert.";
               SET @english = "Your data has been saved successfully.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.82
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "uploadTitleImage";
               SET @german  = "Lade dein Titelbild hoch";
               SET @english = "Upload your title image";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.83
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "uploadProfilePicture";
               SET @german  = "Lade dein Profilbild hoch";
               SET @english = "Upload your profile picture";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.84
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "enterYourContactData";
               SET @german  = "Trage die Kontaktdaten ein";
               SET @english = "Enter the contact details";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.85
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "organisationName";
               SET @german  = "Name der Einrichtung";
               SET @english = "Name of your Organization";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.86
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "editOrganisation";
               SET @german  = "Einrichtung bearbeiten";
               SET @english = "Edit Organization";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.87
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createNewOrganisation";
               SET @german  = "Neue Einrichtung erstellen";
               SET @english = "Create new organisation";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.88
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "userInformation";
               SET @german  = "Deine Daten";
               SET @english = "Your data";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.89
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "mediathek";
               SET @german  = "Mediathek";
               SET @english = "Media library";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.90
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "cardImage";
               SET @german  = "Kartenbild";
               SET @english = "Card Image";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.91
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "userName";
               SET @german  = "Dein Name";
               SET @english = "Your name";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0004.add_data_labels_3.92
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "editCategory";
               SET @german  = "Kategorie bearbeiten";
               SET @english = "Edit category";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.93
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createNewCategory";
               SET @german  = "Neue Kategorie erstellen";
               SET @english = "Create new category";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.94
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "editType";
               SET @german  = "Rubrik bearbeiten";
               SET @english = "Edit category";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.95
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createNewType";
               SET @german  = "Neue Rubrik erstellen";
               SET @english = "Create new category";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.96
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createNewDeal";
               SET @german  = "Neue Kleinanzeige erstellen";
               SET @english = "Create new deal";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.97
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "editDeal";
               SET @german  = "Kleinanzeige bearbeiten";
               SET @english = "Edit deal";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.98
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "contactInfo";
               SET @german  = "Kontaktinformation";
               SET @english = "Contact info";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.99
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "dealIsPublic";
               SET @german  = "Kleinanzeige veröffentlichen";
               SET @english = "Publish deal";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.100
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "offer";
               SET @german  = "Angebot";
               SET @english = "Offer";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.101
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "request";
               SET @german  = "Gesuch";
               SET @english = "Request";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.102
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "url";
               SET @german  = "URL";
               SET @english = "URL";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0004.add_data_labels_3.103
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "createNewMedia";
               SET @german  = "Neue Medien erstellen";
               SET @english = "Create new media";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0004.add_data_labels_3.104
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "authorMedia";
               SET @german  = "Urheber:in";
               SET @english = "Author";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";        
               
  - changeSet:
      id: 0004.add_data_labels_3.105
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "price";
               SET @german  = "Preis";
               SET @english = "Price";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";     
               AND l.locale = "en";     
               
  - changeSet:
      id: 0004.add_data_labels_3.106
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "organisationApproval";
               SET @german  = "Genehmigung der Organisationen";
               SET @english = "Organisation approval";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0004.add_data_labels_3.107
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "newOrganisations";
               SET @german  = "Neue Organisationen";
               SET @english = "New organisations";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
               