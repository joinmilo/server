databaseChangeLog:
  - logicalFilePath: path_independent                
  - changeSet:
      id: 0002.add_data_labels.1
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "giveUsFeedback";
               SET @german  = "Gib uns dein Feedback!";
               SET @english = "Give us your feedback!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
                
  - changeSet:
      id: 0002.add_data_labels.2
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "needYourFeedback";
               SET @german  = "Um die Seite bestmöglich an eure Wünsche und Bedürfnisse anzupassen, brauchen wir euer Feedback!";
               SET @english = "In order to adapt the site to your wishes and needs in the best possible way, we need your feedback!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0002.add_data_labels.3
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "featureRequest";
               SET @german  = "Gerne kannst du uns auch deine Vorschläge für neue Features zusenden, damit wir diese in Zukunft umsetzen können.";
               SET @english = "You are also welcome to send us your suggestions for new features so that we can implement them in the future.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.4
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "reportReceived";
               SET @german  = "Dein Report is bei uns angekommen. Wir kümmern uns bald darum!";
               SET @english = "Your report has reached us. We'll take care of it soon!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.5
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "criticalError";
               SET @german  = "Schwerwiegender Fehler! Bitte Support kontaktieren.";
               SET @english = "Fatal error! Please contact support.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.6
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "tryAgain";
               SET @german  = "Bitte erneut probieren.";
               SET @english = "Please try again.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.7
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "sharedContent";
               SET @german  = "Geteilte Inhalte";
               SET @english = "Shared content";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.8
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "dashboard";
               SET @german  = "Dashboard";
               SET @english = "Dashboard";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.9
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "settings";
               SET @german  = "Einstellungen";
               SET @english = "Settings";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.10
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "becomeContentCreator";
               SET @german  = "Content Creator werden";
               SET @english = "Become content creator";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.11
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "manageContent";
               SET @german  = "Inhalte verwalten";
               SET @english = "Manage content";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0002.add_data_labels.12
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               SET @tagId   = "articleReceived";
               SET @german  = "Dein Artikel is bei uns angekommen. Wir kümmern uns bald darum!";
               SET @english = "Your article has reached us. We'll take care of it soon!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0002.add_data_labels.13
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "yourStory";
               SET @german  = "Deine Story";
               SET @english = "Your Story";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0002.add_data_labels.14
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "basicData";
               SET @german  = "Grunddaten";
               SET @english = "Basic Data";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0002.add_data_labels.15
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "article";
               SET @german  = "Artikel";
               SET @english = "Article";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";     
               
  - changeSet:
      id: 0002.add_data_labels.16
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "article";
               SET @german  = "Artikel";
               SET @english = "Article";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0002.add_data_labels.17
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "informations";
               SET @german  = "Informationen";
               SET @english = "Informations";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.18
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "registrationReceived";
               SET @german  = "Vielen Dank für Ihre Registrierung! Wir haben Ihre Daten erhalten und senden Ihnen in Kürze eine Bestätigung per E-Mail.";
               SET @english = "Thank you for your registration! We have received your data and will shortly send you a confirmation by e-mail.";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";

  - changeSet:
      id: 0003.add_data_labels.19
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "firstName";
               SET @german  = "Vorname";
               SET @english = "First name";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0003.add_data_labels.20
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "lastName";
               SET @german  = "Nachname";
               SET @english = "Last name";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0003.add_data_labels.21
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "haveAcc?";
               SET @german  = "Bereits einen Account?";
               SET @english = "Already have an account?";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0003.add_data_labels.22
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "congratulations";
               SET @german  = "Glückwunsch!";
               SET @english = "Congratulations!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";        
               
  - changeSet:
      id: 0003.add_data_labels.23
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "verificationSuccess";
               SET @german  = "Du hast deine E-mail erfolgreich verifiziert.";
               SET @english = "You have successfully verified your email.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en"; 
               
  - changeSet:
      id: 0003.add_data_labels.24
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "troubleLogin";
               SET @german  = "Probleme beim Anmelden?";
               SET @english = "Trouble logging in?";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.25
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "backToLogin";
               SET @german  = "Zurück zur Anmeldung";
               SET @english = "Back to login";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en"; 
               
  - changeSet:
      id: 0003.add_data_labels.26
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "calendarDescription";
               SET @german  = "Keine Veranstaltung mehr verpassen!";
               SET @english = "Never miss an event again!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
            
  - changeSet:
      id: 0003.add_data_labels.27
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "dashboardDescription";
               SET @german  = "Dein persönliches Dashboard!";
               SET @english = "Your personal dashboard!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";             

  - changeSet:
      id: 0003.add_data_labels.28
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "friendDescription";
               SET @german  = "Mit Freunden in Verbindung bleiben!";
               SET @english = "Stay in touch with friends!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.29
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "notificatonsDescription";
               SET @german  = "Individuelle Erinnerungen einstellen!";
               SET @english = "Set custom reminders!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en"; 
               
  - changeSet:
      id: 0003.add_data_labels.30
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privacyPolicies";
               SET @german  = "Datenschutzrichtlinien";
               SET @english = "Privacy Policies";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.31
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "mailSentSucessfully";
               SET @german  = "Die E-mail wurde erfolgreich versandt.";
               SET @english = "The email was sent successfully.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.32
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "resetPasswordSuccess";
               SET @german  = "Das Passwort wurde erfolgreich geändert.";
               SET @english = "The password was succesfully changed.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                                         
                                     
  - changeSet:
      id: 0003.add_data_labels.33
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "failure";
               SET @german  = "Etwas lief schief!";
               SET @english = "Something went wrong!";


               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";        
               
  - changeSet:
      id: 0003.add_data_labels.34
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "verificationFailure";
               SET @german  = "Bitte versuche es erneut, oder kontaktiere den Support.";
               SET @english = "Please try again, or contact the support.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en"; 
               
  - changeSet:
      id: 0003.add_data_labels.35
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privacyPolicy";
               SET @german  = "Ich akzeptiere die Datenschutzrichtlinien.";
               SET @english = "I accept the privacy policies.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                 
                                         
  - changeSet:
      id: 0003.add_data_labels.36
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "registerInfotext";
               SET @german  = "Erstelle einen Account, um in deiner Umgebung auf dem Laufenden zu bleiben";
               SET @english = "Create an account to stay up to date in your area";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.37
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "notRegistered?";
               SET @german  = "Noch nicht registriert?";
               SET @english = "Not yet registered?";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";               
                                        
  - changeSet:
      id: 0003.add_data_labels.38
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "writeComment";
               SET @german  = "Eigenen Kommentar schreiben";
               SET @english = "Write your own comment";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   

  - changeSet:
      id: 0003.add_data_labels.39
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "allComments";
               SET @german  = "Alle Kommentare ansehen";
               SET @english = "View all comments";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";            
               
  - changeSet:
      id: 0003.add_data_labels.40
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "addAComment";
               SET @german  = "Füge einen Kommentar hinzu";
               SET @english = "Add a comment";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.41
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "feedbackComment";
               SET @german  = "Was hat dich begeistert oder eher gestört?";
               SET @english = "What excited you or rather bothered you?";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.42
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "cancel";
               SET @german  = "Abbrechen";
               SET @english = "Cancel";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en"; 
               
  - changeSet:
      id: 0003.add_data_labels.43
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "commentSaved";
               SET @german  = "Ihr Kommentar wurde erfolgreich gespeichert.";
               SET @english = "Your comment was saved successfully.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.44
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "attend";
               SET @german  = "Teilnehmen";
               SET @english = "Attend";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";              
               
  - changeSet:
      id: 0003.add_data_labels.45
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "attendingFriends";
               SET @german  = "Teilnehmende Freunde";
               SET @english = "Attending Friends";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                                                                                                                                                
                                                   
  - changeSet:
      id: 0003.add_data_labels.46
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "download";
               SET @german  = "Download";
               SET @english = "Download";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.47
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "inviteFriends";
               SET @german  = "Freunde einladen";
               SET @english = "Invite Friends";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.48
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "showAllFriends";
               SET @german  = "Alle Freunde anzeigen";
               SET @english = "Show all friends";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0003.add_data_labels.49
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "toTheOrganisation";
               SET @german  = "Zum Veranstalter";
               SET @english = "Go to Organisation";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.50
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "friends";
               SET @german  = "Freunde";
               SET @english = "Friends";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";         
               
  - changeSet:
      id: 0003.add_data_labels.51
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "allNotifications";
               SET @german  = "alle Benachrichtigungen";
               SET @english = "all Notifications";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   

  - changeSet:
      id: 0003.add_data_labels.52
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "allRead";
               SET @german  = "alle gelesen";
               SET @english = "read all";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.53
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "media";
               SET @german  = "Medien";
               SET @english = "Media";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   

  - changeSet:
      id: 0003.add_data_labels.54
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "allMedia";
               SET @german  = "Alle Medien";
               SET @english = "All media";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                          
                                            
  - changeSet:
      id: 0003.add_data_labels.55
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "userReviews";
               SET @german  = "Nutzerrezension";
               SET @english = "User reviews";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";     
               
  - changeSet:
      id: 0003.add_data_labels.56
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "submitReview";
               SET @german  = "Bewertung abgeben";
               SET @english = "Submit review";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                              
                                    
  - changeSet:
      id: 0003.add_data_labels.57
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "ratingSaved";
               SET @german  = "Ihre Bewertung wurde erfolgreich gespeichert.";
               SET @english = "Your review was saved successfully.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.58
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "ratings";
               SET @german  = "Bewertungen";
               SET @english = "Ratings";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.59
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "stars";
               SET @german  = "Sterne";
               SET @english = "stars";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                                                                                                                                                                                                                                        
                                       
  - changeSet:
      id: 0003.add_data_labels.60
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "rating";
               SET @german  = "Bewertung";
               SET @english = "rating";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.61
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "authors";
               SET @german  = "Redaktionsteam";
               SET @english = "editorial team";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.62
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deals";
               SET @german  = "Kleinanzeigen";
               SET @english = "deals";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";     
               
  - changeSet:
      id: 0003.add_data_labels.63
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "contest";
               SET @german  = "Wettbewerbe";
               SET @english = "Contests";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                         
               
  - changeSet:
      id: 0003.add_data_labels.64
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "surveysTitle";
               SET @german  = "Umfragen";
               SET @english = "Surveys";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.65
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "star";
               SET @german  = "Stern";
               SET @english = "star";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.66
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "SoFarNoFriendsAreAttending";
               SET @german  = "Bisher nehmen keine Freunde teil";
               SET @english = "So far no friends are attending";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.67
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "toTheRoom";
               SET @german  = "Zum Raum";
               SET @english = "To the room";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
                                        
  - changeSet:
      id: 0003.add_data_labels.68
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noFixedDates";
               SET @german  = "Keine festen Termine";
               SET @english = "No fixed dates";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.69
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "to";
               SET @german  = "bis";
               SET @english = "to";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                
               
  - changeSet:
      id: 0003.add_data_labels.70
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "attendeeSaved";
               SET @german  = "Ihre Anmeldung wurde erfolgreich gespeichert!";
               SET @english = "Your registration has been successfully saved!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.71
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "youAttendToThisEvent";
               SET @german  = "Du nimmst am Event teil";
               SET @english = "You attend to this event";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.72
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "yourRegistrationIsInProcess";
               SET @german  = "Deine Anmeldung ist in Bearbeitung";
               SET @english = "Your Registration is in process";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  

  - changeSet:
      id: 0003.add_data_labels.73
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "unfortunatelyEventFullyBooked";
               SET @german  = "Das Event ist leider ausgebucht";
               SET @english = "Unfortunately, the event is fully booked";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";     
               
  - changeSet:
      id: 0003.add_data_labels.74
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "deRegister";
               SET @german  = "Abmelden";
               SET @english = "De-register";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                  
                      
  - changeSet:
      id: 0003.add_data_labels.75
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "youSuccessfullyDeRegistered";
               SET @german  = "Du hast dich erfolgreich abgemeldet!";
               SET @english = "You have successfully de-registered";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.76
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "mapView";
               SET @german  = "Kartenansicht";
               SET @english = "Map view";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.77
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "addToFavorites";
               SET @german  = "zu Favoriten hinzufügen";
               SET @english = "add to favorites";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.78
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "participateSurvey";
               SET @german  = "An der Umfrage teilnehmen";
               SET @english = "Participate in the survey";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                             
                                                                                                       
  - changeSet:
      id: 0003.add_data_labels.79
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "participateContest";
               SET @german  = "An dem Wettbewerb teilnehmen";
               SET @english = "Participate in the contest";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";             
               
  - changeSet:
      id: 0003.add_data_labels.80
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "team";
               SET @german  = "Team";
               SET @english = "Team";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                  
               
  - changeSet:
      id: 0003.add_data_labels.81
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "toContest";
               SET @german  = "Zum Wettbewerb";
               SET @english = "to Contest";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                                                                                      
               
  - changeSet:
      id: 0003.add_data_labels.82
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "whatIsYourName";
               SET @german  = "Wie lautet dein Name?";
               SET @english = "What's your name?'";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                               
                                        
  - changeSet:
      id: 0003.add_data_labels.83
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "whatIsYourPhoneNumber";
               SET @german  = "Wie lautet deine Telefonnummer?";
               SET @english = "What is your phone number?";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.84
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "next";
               SET @german  = "Weiter";
               SET @english = "Next";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0003.add_data_labels.85
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "previous";
               SET @german  = "Zurück";
               SET @english = "Previous";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";   
               
  - changeSet:
      id: 0003.add_data_labels.86
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "joinAnOrganisation";
               SET @german  = "Tritt einer Organisation bei";
               SET @english = "Join an organisation";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.87
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "whatIs";
               SET @german  = "Wie lautet deine Adresse?";
               SET @english = "What is your address?";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.88
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "done";
               SET @german  = "Fertig!";
               SET @english = "Done!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";      
               
  - changeSet:
      id: 0003.add_data_labels.89
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "toThePortal";
               SET @german  = "Zum Portal";
               SET @english = "To the portal";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.90
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "save";
               SET @german  = "Speichern";
               SET @english = "Save";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.91
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "YourDataWereSavedSuccessfully";
               SET @german  = "Ihre Daten wurden erfolgreich gespeichert!";
               SET @english = "Your data were saved successfully!";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.92
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "applyToBeABlogger";
               SET @german  = "Bewerbe dich als Author.";
               SET @english = "Apply to be a blogger.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";  
               
  - changeSet:
      id: 0003.add_data_labels.93
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "writeSomethingAboutYou";
               SET @german  = "Schreibe etwas über dich...";
               SET @english = "Write Something about you...";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";    
               
  - changeSet:
      id: 0003.add_data_labels.94
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "iApplyAsABlogger";
               SET @german  = "Ich bewerbe mich als Author.";
               SET @english = "I apply as a blogger.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";                                                                                                                                                                                                                                                    
