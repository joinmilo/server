databaseChangeLog:
  - logicalFilePath: path_independent                
  - changeSet:
      id: 0013.add_labels_privilege_approval.1
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privilegeApproval";
               SET @german  = "Privilegiengenehmigung";
               SET @english = "Privilege approval";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_privilege_approval.2
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "assignRole";
               SET @german  = "Rolle zuweisen";
               SET @english = "Assign role";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_privilege_approval.3
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privilegesApplications";
               SET @german  = "Privilegien Bewerbungen";
               SET @english = "Privileges applications";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_privilege_approval.4
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privilegesDescription";
               SET @german  = "Übersicht über alle Bewerbungen";
               SET @english = "Overview of all applications";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_pages.5
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "changeUserRole";
               SET @german  = "User Rolle ändern";
               SET @english = "Change User Role";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_pages.6
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "privilege";
               SET @german  = "Privileg";
               SET @english = "Privilege";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
  - changeSet:
      id: 0013.add_labels_pages.7
      author: codeschluss
      changes:
        - sql:
            comment: Add labels
            splitStatements: true
            sql:
               /*!40101 SET character_set_client = utf8 */;
               /* Set variables */
               SET @tagId   = "noSuitableRoleFound";
               SET @german  = "Es wurde keine passende Rolle mit dem beantragten Privileg gefunden.";
               SET @english = "No suitable role was found with the requested privilege.";

               /* Insert */
               insert into labels (`id`, `tag_id`) values
               (uuid(), @tagId COLLATE utf8mb4_unicode_ci);
                
               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @german COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "de";

               insert into label_translatables (`id`, `parent_id`, `language_id`, `content`)
               select uuid(), p.id, l.id, @english COLLATE utf8mb4_unicode_ci
               from labels p, languages l
               WHERE p.tag_id = @tagId COLLATE utf8mb4_unicode_ci
               AND l.locale = "en";
               
